TODO:

Implement sound effects and music.
Touch support for paddles.
Fix the stuttering:
	Rewrite for using Pixmap or a texture instead of ShapeRenderer, which is "more oriented 
		towards debugging" instead of performance.